<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verificar Email - MiCiudadSV</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .verification-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            padding: 40px;
            max-width: 500px;
            width: 100%;
            text-align: center;
        }
        
        .verification-title {
            color: #333;
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 20px;
        }
        
        .verification-subtitle {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 30px;
            line-height: 1.6;
        }
        
        .info-box {
            background-color: #e3f2fd;
            border: 1px solid #2196f3;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 30px;
            text-align: left;
        }
        
        .info-box h5 {
            color: #1976d2;
            margin-bottom: 15px;
        }
        
        .info-box ul {
            margin-bottom: 0;
            padding-left: 20px;
        }
        
        .code-input {
            width: 100%;
            max-width: 300px;
            height: 60px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            text-align: center;
            font-size: 1.5rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 20px;
            letter-spacing: 5px;
        }
        
        .code-input:focus {
            border-color: #007bff;
            box-shadow: 0 0 0 0.2rem rgba(0,123,255,0.25);
            outline: none;
        }
        
        .verification-btn {
            background: linear-gradient(45deg, #007bff, #0056b3);
            border: none;
            color: white;
            padding: 15px 40px;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            max-width: 300px;
            margin-bottom: 20px;
        }
        
        .verification-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0,123,255,0.3);
        }
        
        .resend-section {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e1e5e9;
        }
        
        .resend-btn {
            background: none;
            border: none;
            color: #007bff;
            text-decoration: underline;
            cursor: pointer;
            font-size: 1rem;
        }
        
        .resend-btn:hover {
            color: #0056b3;
        }
        
        .error-message {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            display: none;
        }
        
        .success-message {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            display: none;
        }
    </style>
</head>
<body>
    <div class="verification-card">
        <h1 class="verification-title">📧 Verificar Email</h1>
        <p class="verification-subtitle">
            Hemos enviado un código de verificación a tu correo electrónico
        </p>
        
        <div class="info-box">
            <h5>📋 Instrucciones:</h5>
            <ul>
                <li>Revisa tu bandeja de entrada</li>
                <li>Busca el correo de "MiCiudadSV"</li>
                <li>Ingresa el código de 6 dígitos</li>
                <li>Haz clic en "Verificar Código"</li>
            </ul>
        </div>
        
        <div class="error-message" id="errorMessage"></div>
        <div class="success-message" id="successMessage"></div>
        
        <form id="verificationForm" method="POST" action="/auth/verificar-email">
            <input type="hidden" name="token" value="<%= token %>">
            <input type="hidden" name="correo" value="<%= correo %>">
            
            <input 
                type="text" 
                name="codigo" 
                class="code-input" 
                placeholder="000000" 
                maxlength="6" 
                pattern="[0-9]{6}" 
                required
                autocomplete="off"
            >
            
            <button type="submit" class="verification-btn">
                ✅ Verificar Código
            </button>
        </form>
        
        <div class="resend-section">
            <p style="color: #666; margin-bottom: 15px;">
                ¿No recibiste el código?
            </p>
            <button type="button" class="resend-btn" onclick="reenviarCodigo()">
                🔄 Reenviar código
            </button>
        </div>
        
        <div style="margin-top: 30px;">
            <a href="/auth/login" style="color: #666; text-decoration: none;">
                ← Volver al login
            </a>
        </div>
    </div>

    <script>
        // Validar que solo se ingresen números
        document.querySelector('.code-input').addEventListener('input', function(e) {
            this.value = this.value.replace(/[^0-9]/g, '');
        });
        
        // Manejar envío del formulario
        document.getElementById('verificationForm').addEventListener('submit', function(e) {
            const codigo = document.querySelector('input[name="codigo"]').value;
            
            if (codigo.length !== 6) {
                e.preventDefault();
                mostrarError('Por favor, ingresa un código de 6 dígitos');
                return;
            }
            
            // Mostrar mensaje de procesamiento
            document.querySelector('.verification-btn').textContent = '⏳ Verificando...';
            document.querySelector('.verification-btn').disabled = true;
        });
        
        // Función para reenviar código
        function reenviarCodigo() {
            const token = document.querySelector('input[name="token"]').value;
            const correo = document.querySelector('input[name="correo"]').value;
            
            fetch('/auth/reenviar-codigo', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: `token=${encodeURIComponent(token)}&correo=${encodeURIComponent(correo)}`
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    mostrarExito('✅ Nuevo código enviado. Revisa tu correo.');
                } else {
                    mostrarError('❌ Error al reenviar el código: ' + data.message);
                }
            })
            .catch(error => {
                mostrarError('❌ Error de conexión. Intenta de nuevo.');
            });
        }
        
        // Funciones para mostrar mensajes
        function mostrarError(mensaje) {
            const errorDiv = document.getElementById('errorMessage');
            errorDiv.textContent = mensaje;
            errorDiv.style.display = 'block';
            document.getElementById('successMessage').style.display = 'none';
        }
        
        function mostrarExito(mensaje) {
            const successDiv = document.getElementById('successMessage');
            successDiv.textContent = mensaje;
            successDiv.style.display = 'block';
            document.getElementById('errorMessage').style.display = 'none';
        }
        
        // Mostrar mensajes de error si existen
        <% if (typeof error !== 'undefined' && error) { %>
            mostrarError('<%= error %>');
        <% } %>
        
        <% if (typeof success !== 'undefined' && success) { %>
            mostrarExito('<%= success %>');
        <% } %>
    </script>
</body>
</html>
